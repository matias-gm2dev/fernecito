{
  "scripts": {
    "ts": "tsc -w",
    "serve": "cd public && web-dev-server --node-resolve --watch --open --app-index index.html",
    "dev": "yarn ts & yarn serve"
  },
  "name": "game-phaser-app",
  "version": "1.0.0",
  "description": "a simple game test",
  "main": "index.js",
  "author": "Matias Diaz",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.1.35",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  }
}
